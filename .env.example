# Copy this file to .env (which should stay out of git) and fill in real values.
# docker-compose reads .env automatically.
POSTGRES_USER=orders
POSTGRES_PASSWORD=changeme-use-a-real-secret
POSTGRES_DB=orders
SECRET_KEY=changeme-use-a-real-secret
